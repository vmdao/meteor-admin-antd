import React from 'react';
import Page from '../../../components/2pages/backend_logo_suggest/create';
import { compose } from 'react-komposer';

function composer(props, onData) {
  onData(null, {
  })
}
export default compose(composer)(Page);


